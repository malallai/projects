host: 192.168.1.28
port: 22
user: root
files: test

web:
  # Define remote location of `{files}/web` (Full path is `/var/www/html/{remote-folder}`)
  remotefolder: .

deploy:
  # Define the location of the ssh public key
  pubkey: /root/.ssh/id_rsa.pub
